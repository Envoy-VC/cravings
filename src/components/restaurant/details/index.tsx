import React from 'react';

import type { Restaurant } from '~/types';

const RestaurantDetails = ({ id, name, image, cuisine }: Restaurant) => {
  return <div></div>;
};

export default RestaurantDetails;
